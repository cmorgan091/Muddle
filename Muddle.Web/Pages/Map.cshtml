@page
@using Muddle.Domain.Models
@model MapModel
@{
    ViewData["Title"] = "Maps page";
}

<h1 class="display-4">Map Examples</h1>
<p>This page shows examples of code that can be used to generate maps and a render of how those maps look</p>

<h1>Empty Map</h1>
<p>A basic 9x5 map</p>
<render-map Map="@(new MapBuilder(9, 5).Build())" show-coordinates="true"></render-map>
<pre><code class="language-csharp">var map = new MapBuilder(9, 5)
    .Build();
</code></pre>

<h1>Paths</h1>
<p>Paths are the primary surface that a character can travel along</p>

<h2>Single Path</h2>
<p>A single path on an 9x5 map, with the path starting at point 1,2 and travelling East for 7 blocks</p>
<render-map Map="@(new MapBuilder(9, 5).AddPath(1, 2, Directions.East, 7).Build())" show-coordinates="true"></render-map>
<pre><code class="language-csharp">var map = new MapBuilder(9, 5)
    .AddPath(1, 2, Directions.East, 7)
    .Build();
</code></pre>

<h2>Crossroad</h2>
<p>Continuing from the first map, we add a second path starting at point 4,1 and travelling South for 3 blocks</p>
<render-map Map="@(new MapBuilder(9, 5)
                     .AddPath(1, 2, Directions.East, 7)
                     .AddPath(4, 1, Directions.South, 3)
                     .Build())" show-coordinates="true"></render-map>
<pre><code class="language-csharp">var map = new MapBuilder(9, 5)
    .AddPath(1, 2, Directions.East, 7)
    .AddPath(4, 1, Directions.South, 3)
    .Build();
</code></pre>

<h2>Fork Map Example</h2>

<p>A simple map shown like a fork</p>

<render-map Map="new MapBuilder(10, 5)
                    .AddPath(0, 2, Directions.East, 10)
                    .AddPath(5, 0, Directions.South, 5)
                    .AddPath(5, 0, Directions.East, 5)
                    .AddPath(9, 4, Directions.West, 5)
                    .AddPath(0, 3, Directions.North, 3)
            .Build()" show-coordinates="true"></render-map>

<pre><code class="language-csharp">var map = var map = new MapBuilder(10, 5)
    .AddPath(0, 2, Directions.East, 10)
    .AddPath(5, 0, Directions.South, 5)
    .AddPath(5, 0, Directions.East, 5)
    .AddPath(9, 4, Directions.West, 5)
    .AddPath(0, 3, Directions.North, 3)
    .Build();
</code></pre>

<h2>CM Map Example</h2>

<p>A map with paths that look like a letter C followed by a letter M</p>

<render-map Map="new MapBuilder(13, 7)
                    .AddPath(1, 1, Directions.East, 3)
                    .AddPath(1, 1, Directions.South, 5)
                    .AddPath(1, 5, Directions.East, 6)
                    .AddPath(6, 5, Directions.North, 5)
                    .AddPath(6, 1, Directions.East, 3)
                    .AddPath(8, 1, Directions.South, 3)
                    .AddPath(8, 3, Directions.East, 3)
                    .AddPath(10, 3, Directions.North, 3)
                    .AddPath(10, 1, Directions.East, 3)
                    .AddPath(12, 1, Directions.South, 5)
                    .Build()" show-coordinates="true"></render-map>

<pre><code class="language-csharp">new MapBuilder(13, 7)
    .AddPath(1, 1, Directions.East, 3)
    .AddPath(1, 1, Directions.South, 5)
    .AddPath(1, 5, Directions.East, 6)
    .AddPath(6, 5, Directions.North, 5)
    .AddPath(6, 1, Directions.East, 3)
    .AddPath(8, 1, Directions.South, 3)
    .AddPath(8, 3, Directions.East, 3)
    .AddPath(10, 3, Directions.North, 3)
    .AddPath(10, 1, Directions.East, 3)
    .AddPath(12, 1, Directions.South, 5)
    .Build();
</code></pre>

<h1>Background Items</h1>
<p>Background items are objects that we cannot interact with directly, but are there for decoration</p>

<h2>Single Item</h2>
<p>A single item on a map</p>
<render-map Map="@(new MapBuilder(9, 5)
                     .AddBackgroundItem(1, 1)
                     .Build())" show-coordinates="true"></render-map>
<pre><code class="language-csharp">var map = new MapBuilder(9, 5)
    .AddBackgroundItem(1, 1)
    .Build()
</code></pre>

<h2>Multiple Items</h2>
<p>Multiple items on a map beside a path</p>
<render-map Map="@(new MapBuilder(9, 5)
                     .AddBackgroundItem(1, 1)
                     .AddBackgroundItem(3, 1)
                     .AddPath(0,2,Directions.East, 7)
                     .AddBackgroundItem(2, 3)
                     .AddBackgroundItem(5, 3)
                     .Build())" show-coordinates="true"></render-map>
<pre><code class="language-csharp">var map = new MapBuilder(9, 5)
    .AddBackgroundItem(1, 1)
    .AddBackgroundItem(3, 1)
    .AddPath(0,2,Directions.East, 7)
    .AddBackgroundItem(2, 3)
    .AddBackgroundItem(5, 3)
    .Build());
</code></pre>
